import 'package:flutter/material.dart';

class HostDashboardProvider extends ChangeNotifier {
  // Provider for any state management in Host Dashboard.
  // Currently handles no major dynamic state, placeholder for future logic.
}
